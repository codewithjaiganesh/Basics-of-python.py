#Given a list of integers compute the average of all numbers in the list.
input_lists = (input("enter the numbers: ").split())
for ints in input_lists :
    sum = "aa"
    sum += ints
    
print(sum)
